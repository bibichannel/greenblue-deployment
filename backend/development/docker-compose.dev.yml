version: '3'
services:
    backend:
      build:
        context: .
        dockerfile: Dockerfile.dev
      image: backend-dev:v1
      restart: unless-stopped
      tty: true
      networks:
        - backend_network

networks:
  backend_network:
    name: gateway_network
    external: true
          